# 필수 의존성 (Core Dependencies)
torch>=2.0.0                    # 딥러닝 프레임워크 (CUDA 지원)
# CUDA 지원을 위해서는 다음 명령어로 설치하는 것을 권장:
# pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
torchvision>=0.15.0             # 컴퓨터 비전 (선택적)
torchaudio>=2.0.0               # 오디오 처리 (선택적)
gymnasium>=0.29.0               # 강화학습 환경
numpy>=1.24.0                   # 수치 계산
matplotlib>=3.7.0               # 기본 시각화
Pillow>=9.0.0                   # 이미지 처리 (matplotlib 백엔드)

# 알고리즘 구현용 (Algorithm Implementation)
scipy>=1.10.0                   # OU 노이즈 프로세스
pandas>=2.0.0                   # 실험 결과 분석
seaborn>=0.12.0                 # 고급 시각화
tqdm>=4.65.0                    # 진행률 표시

# 설정 관리 (Configuration)
pyyaml>=6.0                     # YAML 설정 파일

# 비디오 생성 (Video Generation)  
opencv-python>=4.7.0            # 비디오 처리 및 녹화
imageio>=2.25.0                 # 추가 이미지/비디오 처리
imageio-ffmpeg>=0.4.8           # FFmpeg 통합 (선택)

# 선택적 의존성 (Optional)
tensorboard>=2.13.0             # 학습 시각화 (선택)
jupyter>=1.0.0                  # 대화형 노트북 (선택)
ipython>=8.12.0                 # 향상된 REPL (선택)

# GPU 최적화 의존성 (GPU Optimization)
nvidia-ml-py3>=7.352.0          # GPU 모니터링 (선택적)

# 설치 명령어:
# 1. CPU 버전:
#    pip install -r requirements.txt
#
# 2. GPU 버전 (CUDA 11.8 기준):
#    pip install torch>=2.0.0 torchvision>=0.15.0 torchaudio>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements.txt
#
# 3. GPU 버전 (CUDA 12.1 기준):
#    pip install torch>=2.0.0 torchvision>=0.15.0 torchaudio>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements.txt
#
# 참고: CUDA 버전은 시스템에 설치된 NVIDIA 드라이버와 호환되어야 합니다.
# nvidia-smi 명령어로 CUDA 버전을 확인할 수 있습니다.